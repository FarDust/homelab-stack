[project]
name = "compose"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cluster-utils",
    "firecrawl-py>=4.13.4",
    "pre-commit>=4.5.1",
    "pylance>=1.0.3",
    "redis>=7.1.0",
    "rq>=2.6.1",
    "ruff>=0.14.14",
    "uptime-kuma-api>=1.2.1",
    "websocket-client>=1.9.0",
]

[tool.uv.sources]
cluster-utils = { path = "cluster-utils", editable = true }
