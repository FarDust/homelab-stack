version: "3.9"

networks:
  traefik-public:
    external: true
  keycloak-internal:
    driver: overlay

services:
  keycloak:
    image: quay.io/keycloak/keycloak:26.4.7
    command:
      - start
      - --http-enabled=true
      - --proxy-headers=xforwarded
      - --hostname=https://idp.${LOCAL_DOMAIN?err}
    networks:
      - traefik-public
      - keycloak-internal
    environment:
      - KC_BOOTSTRAP_ADMIN_USERNAME=admin
      - KC_BOOTSTRAP_ADMIN_PASSWORD=${KEYCLOAK_ADMIN_PASSWORD?err}
      - KC_DB=postgres
      - KC_DB_URL=jdbc:postgresql://keycloak-db:5432/keycloak
      - KC_DB_USERNAME=keycloak
      - KC_DB_PASSWORD=${KEYCLOAK_DB_PASSWORD?err}
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: ${RESTART_DELAY_STANDARD}
        max_attempts: ${RESTART_MAX_ATTEMPTS}
        window: ${RESTART_WINDOW_STANDARD}
      update_config:
        order: stop-first
        failure_action: rollback
        monitor: ${UPDATE_MONITOR_STANDARD}
        max_failure_ratio: 0.3
      rollback_config:
        order: stop-first
        monitor: ${ROLLBACK_MONITOR_STANDARD}
      placement:
        constraints:
          - node.role == worker
      labels:
        - "traefik.enable=true"
        - "traefik.http.routers.keycloak.rule=Host(`idp.${LOCAL_DOMAIN?err}`)"
        - "traefik.http.routers.keycloak.entrypoints=websecure"
        - "traefik.http.routers.keycloak.tls=true"
        - "traefik.http.routers.keycloak.tls.certresolver=le-dns"
        - "traefik.http.services.keycloak.loadbalancer.server.port=8080"
        - "traefik.swarm.network=traefik-public"

  keycloak-db:
    image: postgres:17.7
    networks:
      - keycloak-internal
    volumes:
      - keycloak_db_data:/var/lib/postgresql/data
    secrets:
      - source: keycloak_db_password
        target: keycloak_db_password
    environment:
      - POSTGRES_DB=keycloak
      - POSTGRES_USER=keycloak
      - POSTGRES_PASSWORD_FILE=/run/secrets/keycloak_db_password
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: on-failure
        delay: ${RESTART_DELAY_STANDARD}
        max_attempts: ${RESTART_MAX_ATTEMPTS}
        window: ${RESTART_WINDOW_STANDARD}
      update_config:
        order: stop-first
        failure_action: rollback
        monitor: ${UPDATE_MONITOR_STANDARD}
        max_failure_ratio: 0.3
      rollback_config:
        order: stop-first
        monitor: ${ROLLBACK_MONITOR_STANDARD}
      placement:
        constraints:
          - node.role == worker
          - node.labels.storage.manager == true
          - node.labels.load.tier == heavy

volumes:
  keycloak_db_data:
    driver: local

secrets:
  keycloak_admin_password:
    name: keycloak_admin_password-${CONFIG_VERSION:-0}
    file: ../../secrets/keycloak/admin_password
  keycloak_db_password:
    name: keycloak_db_password-${CONFIG_VERSION:-0}
    file: ../../secrets/keycloak/postgres_password
