apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: ${BEEAI_API_ROUTE_NAME}
  namespace: ${BEEAI_NAMESPACE}
spec:
  parentRefs:
    - kind: Gateway
      name: traefik-gateway
      namespace: kube-system
      sectionName: websecure
  hostnames:
    - ${BEEAI_API_HOSTNAME}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - kind: Service
          name: agentstack-server-svc
          port: 8333
