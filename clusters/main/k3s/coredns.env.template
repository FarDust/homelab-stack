# Copy to clusters/main/k3s/coredns.env and fill real values.

# CoreDNS upstream resolver used by k8s pod DNS path.
# For tailscale-only setups, use tailscale DNS.
K3S_COREDNS_FORWARD_UPSTREAM="100.100.100.100"
