node-name: "${K3S_NODE_NAME}"
token: "${K3S_SERVER_TOKEN}"
datastore-endpoint: "${K3S_DATASTORE_ENDPOINT}"
node-ip: "${K3S_NODE_IP}"
advertise-address: "${K3S_NODE_ADVERTISE_ADDRESS}"
flannel-iface: "${K3S_FLANNEL_IFACE}"
