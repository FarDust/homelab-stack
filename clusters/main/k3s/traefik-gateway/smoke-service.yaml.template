apiVersion: v1
kind: Service
metadata:
  name: ${K3S_TRAEFIK_GATEWAY_SMOKE_SERVICE}
  namespace: ${K3S_TRAEFIK_GATEWAY_SMOKE_NAMESPACE}
spec:
  selector:
    app: ${K3S_TRAEFIK_GATEWAY_SMOKE_DEPLOYMENT}
  ports:
    - name: http
      port: 80
      targetPort: 80
