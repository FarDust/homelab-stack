apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ${K8S_SYNC_PVC_NAME}
  namespace: ${K8S_SYNC_NAMESPACE}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: ${K8S_SYNC_STORAGE_CLASS}
  resources:
    requests:
      storage: ${K8S_SYNC_PVC_SIZE}
